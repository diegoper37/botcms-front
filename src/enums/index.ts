export enum WebhookEventType {
  MESSAGE = 'MESSAGE',
  POSTBACK = 'POSTBACK',
}

export enum MessageGroup {
  TEXT = 'TEXT',
  POSTBACK = 'POSTBACK',
}

export enum MessageType {
  TEXT = 'text',
  QUICK_REPLY = 'quick_reply',
}
